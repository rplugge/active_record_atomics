<div id="photos_div">
  <div>
    <% Photo.where(id: params["id"]).each do |photo| %>
      <p><%= Photographer.where(id: photo.photographer_id).first.name %></p>
      <img src=<%= photo.url %>></img>
    <% end %>
  </div>
</div>

<a href="http://localhost:4567/home">Return Home</a>